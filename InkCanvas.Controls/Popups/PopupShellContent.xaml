<UserControl x:Class="Ink_Canvas.Controls.PopupShellContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
             xmlns:controls="clr-namespace:Ink_Canvas.Controls"
             mc:Ignorable="d">
    <Border CornerRadius="8" Background="{DynamicResource FloatingBarBackgroundBrush}"
            BorderBrush="{DynamicResource FloatingBarAccentBrush}" BorderThickness="3">
        <ikw:SimpleStackPanel Margin="-1">
            <controls:PopupTitleBar x:Name="TitleBar" Title="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            <Border Margin="9,0,9,9" BorderBrush="{DynamicResource FloatingBarPopupInnerBorderBrush}"
                    Background="{DynamicResource FloatingBarPopupInnerBackgroundBrush}" BorderThickness="2"
                    CornerRadius="4">
                <Grid Margin="1">
                    <ContentPresenter x:Name="ContentArea" />
                </Grid>
            </Border>
        </ikw:SimpleStackPanel>
    </Border>
</UserControl>
