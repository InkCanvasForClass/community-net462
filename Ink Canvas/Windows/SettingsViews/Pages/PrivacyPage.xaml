<ui:Page xmlns:props="clr-namespace:Ink_Canvas.Properties" x:Class="Ink_Canvas.Windows.SettingsViews.Pages.PrivacyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Ink_Canvas.Windows.SettingsViews.Pages"
      xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
      xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
      xmlns:sys="clr-namespace:System;assembly=mscorlib"
      mc:Ignorable="d"

      Title="{x:Static props:NavStrings.Nav_Privacy}">

    <ScrollViewer PanningMode="VerticalFirst">
        <Grid Margin="59,0,59,0">
            <FrameworkElement.Resources>
                <sys:Double x:Key="SettingsCardSpacing">4</sys:Double>
                <Style x:Key="SettingsSectionHeaderTextBlockStyle"
                    BasedOn="{StaticResource BodyStrongTextBlockStyle}"
                    TargetType="TextBlock">
                    <Style.Setters>
                        <Setter Property="Margin" Value="1,30,0,6" />
                    </Style.Setters>
                </Style>
            </FrameworkElement.Resources>
            <Grid>
                <ikw:SimpleStackPanel MaxWidth="1000"
                    HorizontalAlignment="Stretch"
                    Spacing="{StaticResource SettingsCardSpacing}">

                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}"
                        Text="{x:Static props:AboutStrings.PrivacyAndTelemetry}" />

                    <ui:SettingsExpander Header="{x:Static props:AboutStrings.PrivacyAndTelemetry}"
                            IsExpanded="True"
                            d:Visibility="Visible" d:IsExpanded="True">
                        <ui:SettingsExpander.HeaderIcon>
                            <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Shield}" />
                        </ui:SettingsExpander.HeaderIcon>
                        <ui:SettingsExpander.Items>
                            <ui:SettingsCard ContentAlignment="Left">
                                <CheckBox x:Name="CheckBoxTelemetryPrivacyAccepted"
                                          Content="{x:Static props:AboutStrings.PrivacyAgreement}"
                                          Checked="CheckBoxTelemetryPrivacyAccepted_Checked"
                                          Unchecked="CheckBoxTelemetryPrivacyAccepted_Checked" />
                            </ui:SettingsCard>
                            <ui:SettingsCard Header="{x:Static props:AboutStrings.TelemetryLabel}">
                                <ComboBox x:Name="ComboBoxTelemetryUploadLevel"
                                          SelectionChanged="ComboBoxTelemetryUploadLevel_SelectionChanged">
                                    <ComboBoxItem Tag="0" Content="{x:Static props:AboutStrings.Telemetry_Off}" />
                                    <ComboBoxItem Tag="1" Content="{x:Static props:AboutStrings.Telemetry_Basic}" />
                                    <ComboBoxItem Tag="2" Content="{x:Static props:AboutStrings.Telemetry_Optional}" />
                                </ComboBox>
                            </ui:SettingsCard>
                        </ui:SettingsExpander.Items>
                    </ui:SettingsExpander>

                    <Rectangle Height="48" />

                </ikw:SimpleStackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</ui:Page>
