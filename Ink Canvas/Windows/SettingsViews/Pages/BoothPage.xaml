<ui:Page xmlns:props="clr-namespace:Ink_Canvas.Properties"
    x:Class="Ink_Canvas.Windows.SettingsViews.Pages.BoothPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:helpers="clr-namespace:Ink_Canvas.Windows.SettingsViews.Helpers"
    xmlns:controls="clr-namespace:Ink_Canvas.Controls;assembly=InkCanvas.Controls"
    Title="{x:Static props:BoothStrings.Title}"
    mc:Ignorable="d">
    <ScrollViewer PanningMode="VerticalFirst">
        <Grid Margin="59,0,59,0">
            <FrameworkElement.Resources>
                <sys:Double x:Key="SettingsCardSpacing">4</sys:Double>
                <Style x:Key="SettingsSectionHeaderTextBlockStyle"
                    BasedOn="{StaticResource BodyStrongTextBlockStyle}"
                    TargetType="TextBlock">
                    <Style.Setters>
                        <Setter Property="Margin" Value="1,30,0,6" />
                    </Style.Setters>
                </Style>
            </FrameworkElement.Resources>
            <Grid>
                <ikw:SimpleStackPanel MaxWidth="1000"
                    HorizontalAlignment="Stretch"
                    Spacing="{StaticResource SettingsCardSpacing}">

                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}"
                        Text="{x:Static props:BoothStrings.Title}" />

                    <controls:LabeledSettingsCard
                        Header="{x:Static props:CanvasStrings.Canvas_LaunchSeewoVideoShowcaseForWhiteboardBooth}"
                        Description="{x:Static props:CanvasStrings.Canvas_LaunchSeewoVideoShowcaseForWhiteboardBoothHint}"
                        Icon="{x:Static ui:SegoeFluentIcons.Video}"
                        SwitchName="ToggleSwitchLaunchSeewoVideoShowcaseForWhiteboardBooth"
                        helpers:SettingsBinder.PropertyPath="Canvas.LaunchSeewoVideoShowcaseForWhiteboardBooth" />

                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}"
                        Text="{x:Static props:BoothStrings.CameraDevices}" />

                    <ui:SettingsCard Header="{x:Static props:BoothStrings.CameraDevices}">
                        <ui:SettingsCard.HeaderIcon>
                            <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Video}" />
                        </ui:SettingsCard.HeaderIcon>
                        <ComboBox x:Name="ComboBoxCamera"
                                  MinWidth="200"
                                  SelectionChanged="ComboBoxCamera_SelectionChanged" />
                    </ui:SettingsCard>

                    <ui:SettingsCard Header="{x:Static props:BoothStrings.Rotate}">
                        <ui:SettingsCard.HeaderIcon>
                            <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Rotate}" />
                        </ui:SettingsCard.HeaderIcon>
                        <ComboBox x:Name="ComboBoxRotation"
                                  MinWidth="200"
                                  SelectionChanged="ComboBoxRotation_SelectionChanged">
                            <ComboBoxItem Content="0°" Tag="0" />
                            <ComboBoxItem Content="90°" Tag="1" />
                            <ComboBoxItem Content="180°" Tag="2" />
                            <ComboBoxItem Content="270°" Tag="3" />
                        </ComboBox>
                    </ui:SettingsCard>

                    <ui:SettingsCard Header="{x:Static props:BoothStrings.Resolution_Tooltip}">
                        <ui:SettingsCard.HeaderIcon>
                            <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.FullScreen}" />
                        </ui:SettingsCard.HeaderIcon>
                        <ComboBox x:Name="ComboBoxResolution"
                                  MinWidth="200"
                                  SelectionChanged="ComboBoxResolution_SelectionChanged">
                            <ComboBoxItem Content="640 × 480" Tag="640,480" />
                            <ComboBoxItem Content="1280 × 720" Tag="1280,720" />
                            <ComboBoxItem Content="1920 × 1080" Tag="1920,1080" />
                            <ComboBoxItem Content="2560 × 1440" Tag="2560,1440" />
                        </ComboBox>
                    </ui:SettingsCard>

                    <Rectangle Height="48" />

                </ikw:SimpleStackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</ui:Page>
