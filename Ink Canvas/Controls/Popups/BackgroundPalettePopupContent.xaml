<UserControl x:Class="Ink_Canvas.Controls.BackgroundPalettePopupContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
             xmlns:controls="clr-namespace:Ink_Canvas.Controls;assembly=InkCanvas.Controls"
             xmlns:i18n="clr-namespace:Ink_Canvas.MarkupExtensions"
             mc:Ignorable="d">
    <Grid>
        <controls:PopupShellContent x:Name="Shell" Title="背景设置" d:Visibility="Collapsed" />
        <ContentControl x:Name="InnerContentHost" Visibility="Collapsed" d:Visibility="Visible">
            <ikw:SimpleStackPanel Margin="8">
                <TextBlock Text="白板模式"
                           Foreground="{DynamicResource TextForeground}"
                           FontSize="10" FontWeight="Bold"
                           HorizontalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Border x:Name="WhiteboardModeBtn"
                            Width="60" Height="30"
                            Background="#2563eb"
                            CornerRadius="4"
                            Margin="0,0,8,0">
                        <TextBlock Text="白板"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </Border>
                    <Border x:Name="BlackboardModeBtn"
                            Width="60" Height="30"
                            Background="LightGray"
                            CornerRadius="4">
                        <TextBlock Text="黑板"
                                   Foreground="Black"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </Border>
                </StackPanel>
                <Border Height="1"
                        Background="{DynamicResource SettingsPageBorderBrush}"
                        Margin="0,12,0,12" />
                <TextBlock Text="背景颜色"
                           Foreground="{DynamicResource TextForeground}"
                           FontSize="10" FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,4,0,8" />
                <Border x:Name="BackgroundColorPreview"
                        Width="100" Height="40"
                        BorderThickness="1"
                        BorderBrush="{DynamicResource SettingsPageBorderBrush}"
                        Background="White"
                        CornerRadius="4"
                        Margin="0,0,0,10"
                        HorizontalAlignment="Center" />
                <StackPanel Orientation="Horizontal" Margin="10,0,10,5">
                    <TextBlock Text="R:" Width="20"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextForeground}" />
                    <Slider x:Name="BackgroundRSlider"
                            Minimum="0" Maximum="255" Value="255"
                            Width="150" Margin="5,0,5,0"
                            VerticalAlignment="Center" />
                    <TextBlock x:Name="BackgroundRValue"
                               Text="255" Width="30"
                               VerticalAlignment="Center"
                               TextAlignment="Right"
                               Foreground="{DynamicResource TextForeground}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10,0,10,5">
                    <TextBlock Text="G:" Width="20"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextForeground}" />
                    <Slider x:Name="BackgroundGSlider"
                            Minimum="0" Maximum="255" Value="255"
                            Width="150" Margin="5,0,5,0"
                            VerticalAlignment="Center" />
                    <TextBlock x:Name="BackgroundGValue"
                               Text="255" Width="30"
                               VerticalAlignment="Center"
                               TextAlignment="Right"
                               Foreground="{DynamicResource TextForeground}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10,0,10,5">
                    <TextBlock Text="B:" Width="20"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextForeground}" />
                    <Slider x:Name="BackgroundBSlider"
                            Minimum="0" Maximum="255" Value="255"
                            Width="150" Margin="5,0,5,0"
                            VerticalAlignment="Center" />
                    <TextBlock x:Name="BackgroundBValue"
                               Text="255" Width="30"
                               VerticalAlignment="Center"
                               TextAlignment="Right"
                               Foreground="{DynamicResource TextForeground}" />
                </StackPanel>
                <Button x:Name="ApplyBackgroundColorBtn"
                        Content="应用颜色"
                        Margin="0,10,0,0"
                        Padding="10,5,10,5"
                        Background="#2563eb"
                        Foreground="White"
                        BorderThickness="0"
                        HorizontalAlignment="Center" />
            </ikw:SimpleStackPanel>
        </ContentControl>
    </Grid>
</UserControl>
